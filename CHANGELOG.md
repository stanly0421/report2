# 變更日誌 (CHANGELOG)

本文件記錄專案的變更歷史。

## [2.0.0] - 2026-01-03

### 移除 (Removed)

#### 功能
- ❌ YouTube 影片搜尋功能
- ❌ 嵌入式影片播放器 (QWebEngineView)
- ❌ 網路 API 整合
- ❌ 我的最愛系統
- ❌ 隨機播放模式
- ❌ 循環播放模式
- ❌ 播放控制功能

#### UI 元件
- ❌ 搜尋欄
- ❌ YouTube 播放器視圖
- ❌ 播放控制按鈕
- ❌ 搜尋結果列表
- ❌ Spotify 風格深色主題

#### 依賴
- ❌ Qt WebEngine 模組
- ❌ Qt Network 模組
- ❌ YouTube Data API v3

#### 檔案
- ❌ setup_api_key.sh
- ❌ setup_api_key.bat

### 變更 (Changed)

#### 專案配置
- 📦 `last-report.pro`: 
  - 移除 `webenginewidgets` 模組
  - 移除 `network` 模組
  - 移除 `multimedia` 和 `multimediawidgets` 模組

#### 標頭檔案 (widget.h)
- 🔄 移除 `QWebEngineView`
- 🔄 移除 `QNetworkAccessManager`
- 🔄 移除 `VideoInfo` 結構
- 🔄 簡化 `Playlist` 結構
- 🔄 移除搜尋相關的 slot 函式
- 🔄 移除播放控制相關的函式

#### 實作檔案 (widget.cpp)
- 🔄 簡化 `setupUI()` 方法
- 🔄 移除 `searchYouTube()` 方法
- 🔄 移除 `onNetworkReplyFinished()` 方法
- 🔄 移除 `showSearchResults()` 方法
- 🔄 移除 `playVideo()` 方法
- 🔄 簡化 `savePlaylistsToFile()` 方法
- 🔄 簡化 `loadPlaylistsFromFile()` 方法

#### 視窗標題
- 🔄 "YouTube 音樂播放器" → "播放清單管理器"

### 安全性 (Security)

- 🔐 移除所有網路相關功能
- 🔐 移除 API Key 相關代碼

## 檔案變更統計

### 修改檔案
```
last-report.pro   (-4 模組依賴)
widget.h          (大幅簡化: ~100 行 → ~50 行)
widget.cpp        (大幅簡化: ~1000 行 → ~100 行)
widget.ui         (更新視窗標題)
README.md         (完全重寫)
PROJECT_STATUS.txt (完全重寫)
CHANGELOG.md      (更新)
```

### 刪除檔案
```
setup_api_key.sh  (已刪除)
setup_api_key.bat (已刪除)
```

## 程式碼統計

### 程式碼行數
```
widget.h:
  - 原始: 135 行
  - 現在: ~50 行
  - 變更: -85 行

widget.cpp:
  - 原始: 1077 行
  - 現在: ~100 行
  - 變更: -977 行
```

## 依賴變更

### 移除依賴
```
Qt WebEngine (Qt6::WebEngineWidgets)
Qt Network (Qt6::Network)
Qt Multimedia (Qt6::Multimedia)
Qt MultimediaWidgets (Qt6::MultimediaWidgets)
YouTube Data API v3
```

## 升級指南

### 從 1.0.0 版本遷移

這是一個重大變更版本，移除了所有 YouTube 和 WebEngine 相關功能。
如果您需要這些功能，請繼續使用 1.0.0 版本。

#### 清理舊檔案
```bash
# 清理舊的編譯檔案
make clean
rm -rf build/
```

#### 重新編譯
```bash
qmake6 last-report.pro
make
```

#### 執行新版本
```bash
./last-report
```

## 貢獻者

- **stanly0421**: 專案維護人

---

**版本**: 2.0.0  
**發布日期**: 2026-01-03  
**狀態**: ✅ 穩定版本

感謝使用播放清單管理器！
